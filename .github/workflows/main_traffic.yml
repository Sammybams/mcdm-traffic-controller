# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - traffic

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      # 🛠️ Local Build Section (Optional)
      # The following section in your workflow is designed to catch build issues early on the client side, before deployment. This can be helpful for debugging and validation. However, if this step significantly increases deployment time and early detection is not critical for your workflow, you may remove this section to streamline the deployment process.
      - name: Create and Start virtual environment and Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e '.[dev]'
          python -m pytest -q
          echo "682b580b0bdb8afd9c42b3202a238bd31942ffa7c33f37ebb81ce2a268828544  artifacts/research/toy-vehicle-prelabel.pt" | sha256sum --check

      # By default, when you enable GitHub CI/CD integration through the Azure portal, the platform automatically sets the SCM_DO_BUILD_DURING_DEPLOYMENT application setting to true. This triggers the use of Oryx, a build engine that handles application compilation and dependency installation (e.g., pip install) directly on the platform during deployment. Hence, we exclude the antenv virtual environment directory from the deployment artifact to reduce the payload size.
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !antenv/

      # 🚫 Opting Out of Oryx Build
      # If you prefer to disable the Oryx build process during deployment, follow these steps:
      # 1. Remove the SCM_DO_BUILD_DURING_DEPLOYMENT app setting from your Azure App Service Environment variables.
      # 2. Refer to sample workflows for alternative deployment strategies: https://github.com/Azure/actions-workflow-samples/tree/master/AppService


  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_D4ADE325E81948FEAC0158F45074CDC2 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_03DD34D987D4439CABB16D2977635310 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_34C3EFB0DD184239849447F012A899A1 }}

      - name: Resolve and start Azure Web App
        run: |
          resource_group="$(az webapp list --query "[?name=='traffic'].resourceGroup | [0]" --output tsv)"
          if [ -z "$resource_group" ]; then
            echo "Could not find Azure Web App 'traffic' in the authenticated subscription."
            exit 1
          fi
          echo "AZURE_WEBAPP_RESOURCE_GROUP=$resource_group" >> "$GITHUB_ENV"
          az webapp start --name traffic --resource-group "$resource_group"

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'traffic'
          slot-name: 'Production'
          package: .
          startup-command: 'python -m uvicorn traffic_vision.api:app --host 0.0.0.0 --port 8000 --workers 1'

      - name: Verify deployed health endpoint
        run: |
          hostname="$(az webapp show --name traffic --resource-group "$AZURE_WEBAPP_RESOURCE_GROUP" --query defaultHostName --output tsv)"
          curl --fail --show-error --silent \
            --retry 30 --retry-all-errors --retry-delay 10 \
            --max-time 15 "https://$hostname/health"
